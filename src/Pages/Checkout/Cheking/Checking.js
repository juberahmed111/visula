import React from 'react';
import auth from '../../../firebase.init';

const Checking = (props) => {
    
   

   
 
    return (
        <div>
            <h1>this is {props.length}</h1>
            
        </div>
    );
};

export default Checking;